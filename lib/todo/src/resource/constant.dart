import 'package:flutter/material.dart';

class Constant extends StatelessWidget {
  static const primaryColor = Color(0xFFB68DC3);
  static const sliderColor = Color(0xFFD6CBE7);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
